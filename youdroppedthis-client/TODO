- setup backend
- user registration, login
- setup database
- artwork and canvas endpoints
- websocket
- artwork expiration
- frontend setup
- fullscreen canvas
- pan and zoom, touch enabled
- show artworks on coordinates
- place artwork
- pixel art editor, touch enabled
- get time right!!!
- info about artwork
- collect artwork (improve)
- view profile (collection, stats)
- jump to coordinate
- lazy loading (calculate chunk IDs from vx,vy,w,h,zoom)
- expression in editor
- disappear animation
- improve editor UX (open from canvas, move with canvas, minimize to corner)
- options drawer
- expire logic on client
- user stats (alter tables) ~
- canvas stats
- account page ~
- sql script
- canvas texture (out-of-bounds, )
- user collection artwork options popup (bg-color, max-size, pp/bio, visit location, time data)
- validate pixel data
- particle limit, 1/256 piece of oc?
- collectible after time
- @tanstack/vue-query (for profiles, collections and transactions), invalidate on placement, profile mutations
- test multi canvas
- improve rate limiter
- icons
- email confirm banner
- back button from settings
- expression info, functions info
- last bonus claim by user
- invalidate query data on logout
- validate username
- custom color picker
- deploy on cloud
- email server setup
- canvas creation form
- landing page (refactor)
- calc grid color
- save draft by canvas
- optimize chunk offset
- on placement, check bounds, resolution, is own
- fix possible race condition when collecting or claiming reward
- join profiles on artworks an canvases
- collection and host rewards
  - claim reward endpoint
  - determine collection reward
- moment ending (calc from last_palcement_time + expiration_time)
- now active page (query)
  - return total_count
  - index on last_placement_time
  - set first_placement_time
- moment rate limit
  - artwork 2 more indexes
  - fetch canvas with my recent activity info
  - collection cooldown var
- claim host reward button
- not found canvas
- update canvas server state
- disable editor based on rate limit
- collection rate limits display
- Now page loading and empty state
- migrate db changes
- email template
- share button

- logo
- SEO
